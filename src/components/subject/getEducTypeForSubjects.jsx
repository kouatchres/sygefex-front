import React from "react";
import { Formik, Form } from "formik";
import { StyledForm, StyledButton, ButtonStyled } from "../utils/FormInputs";

const getEducTypeForSubjects = () => {
  const initialValues = {
    range: "",
  };
  return <Formik initialValues={initialValues}>{({ isSubmitting })=>
return(


    
)}</Formik>;
};

export default getEducTypeForSubjects;
